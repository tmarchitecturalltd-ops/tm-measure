import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ltd.tmdesigns.tmmeasure",
  appName: "TM Measure",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
